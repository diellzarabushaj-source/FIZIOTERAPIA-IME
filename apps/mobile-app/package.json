{
  "name": "fizioterapia-ime-mobile-app",
  "version": "1.0.0",
  "private": true,
  "main": "expo/AppEntry",
  "scripts": {
    "start": "expo start",
    "ios": "expo start --ios",
    "android": "expo start --android",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit",
    "check:env": "node scripts/check-env.mjs",
    "generate:assets": "node scripts/generate-assets.mjs",
    "build:preview": "npm run check:env && npm run generate:assets && eas build --profile preview --platform all",
    "build:ios": "npm run check:env && npm run generate:assets && eas build --profile production --platform ios",
    "build:android": "npm run check:env && npm run generate:assets && eas build --profile production --platform android",
    "submit:ios": "eas submit --platform ios",
    "submit:android": "eas submit --platform android"
  },
  "dependencies": {
    "expo": "53.0.0",
    "expo-status-bar": "2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.0",
    "react-native-url-polyfill": "2.0.0"
  },
  "devDependencies": {
    "@types/react": "19.0.10",
    "sharp": "0.34.1",
    "typescript": "5.8.3"
  }
}
